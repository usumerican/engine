{
  "name": "zshogi",
  "version": "0.10.0",
  "private": true,
  "type": "module",
  "scripts": {
    "zig:test": "zig test src/wasm.zig",
    "zig:build": "zig build -Dstrip=true --release=fast --verbose --summary all",
    "zig:wasm": "zig build wasm -Dstrip=true --release=fast --prefix dist --verbose --summary all",
    "dev": "vite",
    "lib": "vite build --mode lib",
    "test": "vitest run",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "eslint": "^9.5.0",
    "prettier": "^3.3.2",
    "vite": "^5.3.1",
    "vitest": "^1.6.0"
  }
}
