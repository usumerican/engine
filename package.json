{
  "name": "engine",
  "version": "0.8.1",
  "private": true,
  "type": "module",
  "scripts": {
    "zig:test": "zig test src/wasm.zig",
    "zig:build": "zig build --verbose --summary all --release=fast",
    "zig:run": "zig build run --release=fast",
    "zig:wasm": "zig build wasm --prefix dist --verbose --summary all --release=small",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lib": "vite build --config vite-lib.config.js"
  },
  "devDependencies": {
    "eslint": "^9.4.0",
    "prettier": "^3.3.0",
    "vite": "^5.2.12"
  }
}
